# ANRI Task Management System — Project Context

Living handoff document for future work on this project (including drafting an SOP). Not user-facing documentation — this is internal context: what's built, why key decisions were made, and what's still open. Written for a Claude session picking this up cold.

## What this is

A task relay/handoff management system built for a real company (ANRI / RCMC BNLMP Management), intended to eventually be sold/delivered to the client. Core model: tasks move through a chain of assignees ("relay") rather than being assigned to one person; role-based dashboards; full audit trail.

**Stack**: Next.js 16.3.0 (App Router, Turbopack, Server Components/Actions), Supabase (Postgres + Auth + Storage), Tailwind CSS v4, FullCalendar v6.1.21.

**Important**: This Next.js version has breaking changes vs. training-data knowledge (`proxy.ts` replaces `middleware.ts`, `next/script` required for inline scripts, etc.). Docs live in `node_modules/next/dist/docs/` — check there before assuming standard Next.js behavior. See `AGENTS.md` at repo root.

## Deployment

**Live** at `https://anri-tasks.vercel.app`. Source is on GitHub at `github.com/Joooban/anri_tasks` (`main` branch) — Vercel auto-deploys on every push to `main`. Smoke-tested post-deploy (sign-in, task creation, announcements, attachment upload/download round-trip through encryption) and confirmed working.

Three places had to be told about the production URL, and any *future* domain change (e.g. a custom domain) needs the same three updated again:
1. **Vercel env var** `NEXT_PUBLIC_SITE_URL` — set to the production URL.
2. **Supabase → Authentication → URL Configuration** — "Site URL" changed from `http://localhost:3000` to the production URL, and the production URL added to the "Redirect URLs" allowlist (kept the localhost entry too, so local dev still works).
3. Google Cloud Console did **not** need updating — sign-in goes through Supabase's managed Google OAuth, so the redirect URI registered there points at Supabase's own fixed callback (`https://<project-ref>.supabase.co/auth/v1/callback`), not this app's domain.

All secrets (`SUPABASE_SERVICE_ROLE_KEY`, `ENCRYPTION_KEY`, the Supabase URL/anon key) are set as Vercel environment variables — server-only ones (service role, encryption key) are not exposed to the client since they're never `NEXT_PUBLIC_`-prefixed. All 22 migrations (`0001`–`0022`) have been applied to the Supabase project this deployment points at.

**Onboarding a real person now requires two steps in order**: add their email to the allowlist (`/accounts` → "Allowed emails", boss_boss/supervisor only) *before* they attempt to sign in — signing in first will fail with "not approved yet" and they'll need to retry after being added. This is a reversal of the pre-allowlist flow ("sign in, then get set up") worth calling out explicitly in the SOP.

## CSP, task type admin UI, generated types (this session)

**Content-Security-Policy**: implemented as a strict, nonce-based policy rather than the `'unsafe-inline'` shortcut, since every page here already reads cookies (`createClient()` → `cookies()`), which forces dynamic rendering anyway — normally nonce-CSP's main cost, but already paid for here regardless. `src/lib/supabase/middleware.ts` generates a fresh nonce per request, sets it as both the `x-nonce` request header and part of the `Content-Security-Policy` response header; the root layout (`src/app/layout.tsx`, now `async`) reads `x-nonce` via `headers()` and passes it to the theme-init `<Script>`. `script-src` is strict (nonce + `'strict-dynamic'`, no `'unsafe-inline'`). `style-src` still needs `'unsafe-inline'` — confirmed by reading FullCalendar's bundled source that it injects a `<style>` tag at runtime with no nonce awareness (`@fullcalendar/core`'s `injectStyles()`); a strict `style-src` would silently break the calendar's base layout. If you ever add a third-party script/library, check whether it also does runtime DOM injection like this before assuming a stricter policy is safe.

**Task type admin UI**: added as a third card on `/accounts` (`TaskTypesManager` component) alongside Allowed Emails and the accounts table — add/delete task types without SQL. Uses the existing `task_types_write` RLS policy directly (not a new RPC): unlike the policies removed in `0019`, this one was already correctly scoped (`is_boss_or_supervisor()` on both `USING` and `WITH CHECK`, no column-tampering risk), so it didn't need hardening.

**Generated TypeScript types**: `src/lib/database.types.ts` is now generated from the live schema (`supabase gen types typescript --project-id dhbjfbdvsybwbbaxmnad`, needs a Supabase **personal access token** — different from the project API keys already in `.env.local`, generate one at supabase.com/dashboard/account/tokens, ideally revoke it after use since it's account-wide) and wired into all three Supabase client factories (`src/lib/supabase/client.ts`, `server.ts`, `service-role.ts`) via the generic type param. **Regenerate this file whenever the schema changes** (new migration adds/changes a table or RPC) — it will drift silently otherwise, defeating the point.

Wiring it in immediately surfaced real mismatches between the hand-maintained `src/lib/types.ts` and the actual schema, all now fixed — useful to know the shape of these if it happens again after a future regeneration:
- **`text` columns constrained only by a CHECK constraint** (`role`, task `status`) generate as plain `string`, not the narrower `Role`/`TaskStatus` union the app uses everywhere. Fixed by narrowing once at the query source (`getCurrentProfile`, `getProfiles`, `getTaskDetail`) rather than at every call site.
- **RPC scalar parameters can't be expressed as nullable** by the generator — Postgres has no "not null" concept for plain function parameters the way it does for columns, so `create_announcement_rpc`'s `p_expires_at` (genuinely nullable — an announcement can have no expiry) still generates as required `string`. Fixed with a small helper, `nullableRpcArg()` in `src/lib/rpc-utils.ts`, used at every RPC call site that legitimately passes `null`.
- **View columns generate as nullable** regardless of the underlying source data's actual nullability (`task_departments.task_id`) — handled with a type-guard filter rather than a blind assertion, so it stays correct if that view's guarantees ever actually change.

## Client feedback batch: task-status fix, History→Activity Log, dashboard consolidation, Company Overview features (this session)

The client sent a structured feedback document (General/Dashboard/History/Calendar/Tasks/Company Overview/Accounts) plus a diagram sketching a "share task via link" flow. Per explicit instruction ("run it by me first"), the two architecturally large items were deferred rather than built — see "Deferred, not yet started" below. Everything else was scoped, approved piecemeal by the user, and implemented:

- **Task status bug fix** (`0023_fix_initial_task_status.sql`): `create_task_rpc` inserted a task's first chain step directly as `'active'` (an INSERT), but the trigger that advances `tasks.status` only fires `after update of status` — never on INSERT. A single-step chain's task jumped straight `to_do` → `done`, skipping `in_progress` entirely; a multi-step chain showed `to_do` for the whole time the first assignee worked on it. Fixed by setting `status = 'in_progress'` explicitly in the `tasks` INSERT itself, plus a one-time data-correction UPDATE for already-affected rows.
- **History renamed to "Activity Log"** in the nav and page heading only — the route is still `/history`, not renamed, to avoid a needless redirect/bookmark break for a label-only change.
- **Department Health widget retired** — it and Department Tiles showed near-duplicate info (client called this out directly). `department-health-grid.tsx` deleted; `getDepartmentHealthGrid()` itself stays, since Department Tiles and the `/departments` Company Overview page both still consume its data for their own display.
- **Overdue & Blocked + Upcoming Deadlines merged into one "Needs Attention" widget** — `getNeedsAttentionTasks()` in `dashboard-queries.ts` replaces both old queries, one merged/ranked (overdue > blocked > due-soon) list instead of two competing widgets. Applied to both the Boss/Supervisor dashboard and the department-head dashboard for consistency.
- **Completion Rate**: un-widened back to half-width (it had been forced full-width earlier this session to avoid an awkward pairing — the client's ask to resize Announcements to match it prompted resizing Completion Rate itself instead, per the user's explicit call), and a "Today" stat added alongside week/month (`getCompletionRate(1, ...)`).
- **Announcements**: now a one-at-a-time carousel (prev/next + dot indicators, small CSS slide-in on navigation — `@keyframes announcement-slide-in` in `globals.css`) instead of a scrolling list. Default expiry changed to 24h after publish unless a custom expiry is set (both create and update — previously blank meant "never expires").
- **Calendar**: task deadlines vs. meetings/events are now visually distinguished by **marker shape** (circle vs. 45°-rotated diamond) rather than color alone, since icons/emoji are off the table app-wide. Explained in the updated legend.
- **Tasks list**: added a department filter dropdown (mirrors History's), submits via a plain GET form — no client component needed.
- **Company Overview nav position**: moved above Tasks, directly below Dashboard.
- **Company Overview page** (`/departments`) gained three new sections:
  - **Completion rate trend chart** — `getCompletionRateTrend(bucket)` buckets tasks by day(30)/week(12)/month(12)/year(5) using `date-fns` (already a dependency), rendered with a hand-rolled SVG bar chart (`completion-trend-chart.tsx` — no charting library exists in this project, and one chart didn't justify adding one). Bucket choice is a URL param (`?trend=`), same Link-based filter pattern as History/Tasks.
  - **Upcoming birthdays** — new `profiles.birthday_month`/`birthday_day` columns (`0025_profile_birthday.sql`, **month/day only, no year** — deliberately can't compute age). Admin-entered only, via two new inputs in the Accounts table (no self-service profile page exists yet). `getUpcomingBirthdays()` computes days-until in JS, wrapping year-end.
  - **Downloadable document templates** — new `document_templates` table + private `document-templates` Storage bucket (`0026_document_templates.sql`). Deliberately **no write RLS policies** on either the table or the bucket — all writes go through `departments/template-actions.ts` using the service-role client after an app-level role check, the same pattern proven necessary for task attachments (see the RPC-pattern section above) rather than trusting a `WITH CHECK` policy. Downloads use service-role-generated signed URLs (5 min TTL) rather than a streaming route, since templates aren't encrypted (not sensitive per-task data, unlike attachments).

**Generated types**: `document_templates` and `profiles.birthday_month`/`birthday_day` were initially added to `src/lib/database.types.ts` by hand (matching what `supabase gen types` would produce), then verified against the live schema once a token was available — a fresh `supabase gen types typescript` came back byte-for-byte identical to the hand-written version, confirming it was correct.

**Migrations `0023`–`0026` confirmed applied** to production via `supabase db push` (CLI, using a temporary personal access token). Note for next time: since every prior migration (`0001`–`0022`) was applied by pasting SQL into the Dashboard's SQL Editor rather than the CLI, the CLI's own migration-history table had **zero** recorded migrations despite the schema being fully up to date — `supabase migration list` showed all of them as `remote: ""`. Had to `supabase migration repair --status applied 0001 ... 0022` first (backfills the tracking table without re-running anything) before `db push` would correctly see only `0023`–`0026` as pending, rather than trying to re-run the entire history from scratch. **Worth using `supabase db push` for all future migrations** instead of the SQL Editor now that the CLI is linked and the history is in sync — avoids this repair step recurring.

**Deferred, not yet started**: an Admin-defined custom roles/permissions system — would mean redesigning the fixed 4-role authorization model this entire app is built on. Not started; no scope decision made yet.

## Task-sharing via link (this session, post-deploy follow-up)

The client's diagram suggested a full public-preview-and-signup flow for logged-out link recipients. **Scope decision, made explicitly with the user**: share links stay internal-only — no public preview, no new signup path, nothing that touches the email allowlist. Reasoning: opening any part of task content to logged-out visitors, or letting a link holder self-serve a new account, works directly against the allowlist that was just built for a real PII exposure reason (see the email-allowlist section below) — not worth the risk for a feature the client hadn't concretely asked for beyond a generic flow diagram.

Turned out most of "share a task via link" already existed (`CopyForViberButton` on the task detail page already builds a `/tasks/[id]` link + text summary and copies it / opens the native share sheet) — investigating what was actually missing found two real, narrowly-scoped bugs rather than a new feature to build:
- **The login page never read `?next=`.** `updateSession()` in `middleware.ts` has always set `?next=<original path>` when redirecting an unauthenticated visitor to `/login` — but `login/page.tsx`'s `signIn()` never read it, so it silently dropped on the floor and every login landed on `/dashboard` regardless of where the user was trying to go. A shared link opened while logged out (or after a session expired) never actually returned the person to the task. Fixed: `login/page.tsx` now reads `next` via `useSearchParams()` (wrapped in `<Suspense>`, required for that hook) and forwards it as `?next=` on the `redirectTo` sent to Supabase OAuth; `/auth/callback/route.ts` already read and used a `next` param correctly, it just never received a real one before.
- **Unauthorized task access was a bare 404.** `getTaskDetail()` returns `null` both when a task genuinely doesn't exist and when RLS (`can_view_task`) blocks it for the current viewer — `tasks/[id]/page.tsx` couldn't distinguish the two and always called `notFound()`. A shared link landing on a colleague without access to that specific task looked like a dead/broken link. Fixed with a service-role existence check only in the `!detail` branch: if the task genuinely doesn't exist, still a real 404; if it exists but access is denied, a clear "You don't have access to this task" message instead. Deliberately reveals task *existence* (not content) to an authenticated-but-unauthorized viewer — an accepted, explicit tradeoff for this small internal system, not something to reuse blindly for a public-facing feature later.

## Roles & standing design principles

Roles: `employee` < `department_head` < `boss_boss` / `supervisor` (president-level).

**Boss/Supervisor are visibility-only, never a "final call" override on other people's task work.** This was explicitly corrected by the client/user after an early implementation gave them override power on relay confirmations — reverted in favor of a self-resolve pattern (see `0017_self_resolve_unconfirmable_step.sql`). They *can* block/unblock tasks and moderate announcements (delete, not edit), but cannot confirm/complete someone else's step or edit someone else's content. **Respect this distinction in all future features** — it's a decision the client pushed back hard on once already.

Employees are read-only on task creation / announcements — they can't create tasks, post announcements, or schedule meetings.

## Architecture: the SECURITY DEFINER RPC pattern (important — read this before adding any write path)

Every write in this app (create task, post announcement, relay actions, deletes, etc.) goes through a Postgres `SECURITY DEFINER` PL/pgSQL RPC function, **not** plain RLS `INSERT`/`UPDATE` policies with `WITH CHECK`.

Why: early in the build, RLS `WITH CHECK` policies referencing role/department (e.g. `tasks_insert`, `task_assignees_update`) mysteriously rejected valid writes for every non-`boss_boss` account, despite every individual condition being independently verified true via debug functions. The root cause was never found after extensive isolation testing. The fix was architectural: move authorization logic out of RLS and into RPC functions that do their own `auth.uid()`-based validation in PL/pgSQL, then write as the function owner (bypassing RLS by design).

**If you add a new write path, follow this pattern** — don't reach for a plain RLS-gated insert/update, it has a known history of silently failing for non-president accounts. See migrations `0013`–`0015`, `0018` for the established shape (validate role/ownership in `plpgsql`, `raise exception` on failure, `grant execute ... to authenticated`).

**Exception — Supabase Storage**: file uploads can't go through a Postgres RPC, since the bytes go through Supabase's Storage API rather than PostgREST. Same risk (`storage.objects` and `task_attachments` both had `WITH CHECK (... and can_view_task(...))`, structurally identical to the policies that broke elsewhere, and were never tested against a non-`boss_boss` account). The equivalent fix, applied to the attachment-upload path in `tasks/actions.ts`: a **service-role Supabase client** (`src/lib/supabase/service-role.ts`, using `SUPABASE_SERVICE_ROLE_KEY`) that bypasses RLS entirely, called only after the Server Action has already verified the user is authorized (mirrors the RPC pattern's spirit — authorize in trusted server code, then write with elevated privileges). **This key must never get a `NEXT_PUBLIC_` prefix and must never be sent to the client** — it bypasses all RLS for the whole project. It's in `.env.local` (gitignored, confirmed) but has no production home yet — needs to be added to whatever hosting platform's env vars when this deploys.

## Timezone handling pattern

Any `datetime-local` input must convert to a full ISO timestamp **client-side** before submission — see `src/components/ui/local-datetime-field.tsx`. Parsing a bare `datetime-local` string server-side applies the *server's* timezone, not the user's, which silently breaks the moment client and server run in different zones (e.g. once deployed to Vercel while users are in the Philippines). This component now also supports `defaultValueIso` for pre-filling edit forms (added when building announcement editing).

The History page's date-only filters (`<input type="date">`) intentionally do **not** use this pattern — a few hours of fuzziness at the day boundary was judged an acceptable tradeoff for a history filter, unlike a task deadline. Documented in a comment in `history/page.tsx`.

## A critical operational gotcha: migrations are not auto-applied

**Migration files in `supabase/migrations/` are written locally but must be manually pasted into the Supabase Dashboard → SQL Editor and run by the user.** There is no CI/CD or `supabase db push` wired up (no `SUPABASE_ACCESS_TOKEN` available in this environment). This has caused real bugs twice already:

1. `0005_announcement_publish_at.sql` / `0006_announcement_expires_at.sql` were written but never run — announcements were completely broken (`column "publish_at" does not exist`) until caught and the user manually ran the SQL.
2. Same risk exists for `0018_announcement_and_task_delete_rpcs.sql` (announcement edit/delete, cancelled-task hard-delete) — confirmed applied and working as of this writing, but this class of bug **will recur** for any future migration unless a process is put in place.

**This is a strong candidate for the SOP being planned**: a checklist step of "did you run the migration in Supabase?" every time a migration file is added, or ideally some tooling to make this less manual.

## Feature inventory (what's built)

- **Auth**: Google OAuth via Supabase. **Not Workspace-domain-restricted** — this company has no shared Workspace domain, employees sign in with their own personal email addresses, so `NEXT_PUBLIC_GOOGLE_WORKSPACE_DOMAIN` will likely stay blank permanently (not a "not yet set" gap, a correction from an earlier wrong assumption). Access is instead gated by an **email allowlist** (`0022_email_allowlist.sql`) — see the dedicated section below.
- **Task relay workflow**: create task with an ordered assignee chain, complete/confirm/block/unblock steps, auto-advance trigger, cancel task, comments, attachments (Supabase Storage), audit log. Terminal-step confirmation deadlock fixed (`0016`, `0017`).
- **Task deletion**: cancelled tasks can now be permanently, irreversibly deleted (creator or Boss/Supervisor only) — task detail page and History page both have a "Delete permanently" button. New as of this session (`0018`).
- **Announcements**: create (with optional scheduled `publish_at` and `expires_at`), and as of this session, **edit** (author only) and **delete** (author or Boss/Supervisor as moderation) — inline edit form in `announcements-feed.tsx`.
- **Dashboards**: role-based (Boss/Supervisor company-wide dashboard with configurable/reorderable widgets; department-head dashboard). Boss/Supervisor can "View as" a department (read-only preview, `getPreview()`).
- **Calendar**: FullCalendar month/week/list views, task deadlines + meetings. Several rendering bugs fixed this session (see below).
- **History page**: filterable log of done/cancelled tasks. Filters by department, date range, and a "Filter by" toggle — **Completion date** (`updated_at`, default) or **Deadline** — added this session after the completion-vs-deadline distinction caused real user confusion (see "Recently fixed"). Each row shows both "Updated" and "Due" dates when a deadline exists, so it's clear which one drove the match.
- **Accounts admin page**: role/department management, restricted appropriately by role.
- **PWA**: service worker (`public/sw.js`) for offline viewing of already-visited pages — GET-only caching, network-first with cache fallback. Offline *editing* is explicitly out of scope (phase-2, per code comment).
- **Theme**: dark/light toggle via `.dark` class on `<html>` (not `prefers-color-scheme` alone), flash-of-wrong-theme prevented via `next/script` with `strategy="beforeInteractive"` (see "Recently fixed").

## Recently fixed (this session — useful history, avoid re-breaking these)

- **Calendar wide-bar rendering**: FullCalendar has a known inconsistency (`fullcalendar/fullcalendar#5634`) where some day-grid events render as full-width filled bars (`fc-h-event`, block mode) instead of compact dots, even with `eventDisplay="list-item"` set. Root cause fully diagnosed: block-mode events get `display: block`, and `width: auto` on a block element *fills its container* rather than shrink-wrapping — an earlier fix attempt only set `width: auto` and didn't actually fix the visual bug. Real fix: force `display: inline-flex; width: fit-content` in `globals.css` on `.fc-daygrid-event`.
- **Calendar tasks visually spanning into the next day**: task deadline events were `allDay: false` with no explicit `end`, so FullCalendar applied its default 1-hour duration — a deadline late in the day (e.g. 11:30 PM) ended past midnight and rendered as spanning two day cells. Fixed by switching task events to `allDay: true` (FullCalendar's all-day default duration is exactly 1 day, never spans). Note: this meant the client-side "is this overdue" icon logic (which read `arg.event.start.getTime()`, now time-stripped for all-day events) had to be removed rather than patched — turned out to be redundant anyway since the event's dot *color* (computed server-side via `taskCalendarColor`) already encodes overdue/blocked/done state independent of FullCalendar's date handling.
- **Calendar day-of-week header row unreadable in dark mode**: FullCalendar's sticky column-header row has an explicit `background: var(--fc-page-bg-color)` (defaults to white, never themed), so it stayed a solid white bar in dark mode with our page's light dark-mode text rendered on top of it — low contrast. Fixed by re-pointing FullCalendar's own CSS custom properties (`--fc-page-bg-color`, `--fc-border-color`, etc.) under `.dark .fc` in `globals.css`, rather than patching individual selectors.
- **History date filter "doesn't work"**: not actually a bug — user was filtering `updated_at` (completion time) ranges that genuinely had no matching tasks (all test data was completed the evening before the range being tested), and separately expected the filter to match task *deadline* rather than completion time. Confirmed via temporary on-page debug output (added and then removed) showing the raw query bounds against actual data. Resolved by adding a "Filter by: Completion date / Deadline" toggle (see feature inventory above) — the client explicitly chose to add deadline filtering rather than leave it undecided.
- **Announcement posting completely broken**: `column "publish_at" of relation "announcements" does not exist` — see "migrations are not auto-applied" above.
- **Attachment upload never actually hardened against the RLS write-rejection bug**: audited and fixed this session — see the "Exception — Supabase Storage" note under the RPC pattern above. Also found and fixed a real, separate bug along the way: a failed upload was silently swallowed (no error shown, attachment just vanished). Now surfaces as a banner on the task page (`?attachmentError=` query param) instead.
- **`<script>` tag console warning + hydration mismatch**: investigated twice. First pass switched the theme-init script from a raw `<script dangerouslySetInnerHTML>` to `next/script`'s `<Script strategy="beforeInteractive">`, since Next 16 warns about raw script tags even from a Server Component. The warning persisted anyway — traced to Next.js's **own** internal implementation (`node_modules/next/dist/client/script.js`, the `beforeInteractive` + App Router branch), which itself renders `jsx("script", { dangerouslySetInnerHTML })` directly. So this is Next.js's own documented mechanism triggering React's dev-mode warning about its own internals, not a mistake in application code — there is no way to avoid it while using the officially-recommended `beforeInteractive` pattern. The script still executes correctly (Next runs it via a separate `self.__next_s` runtime mechanism, not by relying on React/DOM script-tag semantics). Client decision: leave as-is rather than rework to a cookie-based SSR theme (which would trade away instant OS-dark-mode matching on a visitor's very first, pre-toggle page load). This warning is expected to not appear in production builds, since this class of React dev warning is typically stripped outside development mode. The hydration mismatch in the same report was an unrelated one-time artifact of editing a file live while the user had the page open mid-navigation.
- **Emoji removed app-wide**: replaced with color-coding, text badges (e.g. "Pinned"), or removed where redundant with existing color-coding (calendar event icons were redundant with dot color). Plain `×` (not the dingbat ✕) used for close/remove buttons. User wants no emoji anywhere, including in anything added in future.
- **Base font size increased**: root `font-size: 18px` in `globals.css` (up from browser default 16px) — scales every Tailwind `text-*` utility proportionally since Tailwind v4's type scale is rem-based. One-line, app-wide change rather than touching every component.

## Security review findings (this session) — read before touching auth/RLS

A full manual security review (no GitHub remote exists, so the diff-based `security-review` skill couldn't run against a PR — this was a whole-app manual audit instead) turned up one critical and several other issues, all fixed:

- **Critical — complete privilege escalation**: `profiles_update_self` RLS policy let any authenticated user update their own `profiles` row with no column restriction, including `role`. Since `is_boss_or_supervisor()` and every RPC's authorization check reads `profiles.role` for `auth.uid()`, any employee could grant themselves `boss_boss` with one direct API call, bypassing the Accounts admin page entirely. The policy was never used by any app code (self profile editing was never built) — dropped entirely rather than tightened, in migration `0019`.
- **High — RPC hardening had a gap**: after each write path moved to a `SECURITY DEFINER` RPC (the pattern documented above), the *original* RLS policies those RPCs replaced were never dropped. They remained fully callable directly via the Supabase client SDK with no `WITH CHECK` restricting which columns/values could be written — e.g. `tasks_update` let anyone holding the active step change *any* column on a task directly, not just the status transition the RPC intends; `tasks_delete` let a President delete a task directly without the "must be cancelled first" rule the delete feature is supposed to enforce. Confirmed via grep that no app code depends on any of them (RPCs bypass RLS as the function owner, so dropping the table policy doesn't affect RPC functionality) — all dropped in `0019`: `tasks_update`, `tasks_delete`, `task_assignees_update`, `task_assignees_delete`, `task_visibility_delete`, `task_attachments_delete`, `announcements_delete`, `calendar_events_update`, `calendar_events_delete`.
- **Medium**: no HTTP security headers at all (`next.config.ts` was empty) — added `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy`. Deliberately did **not** add a Content-Security-Policy — that needs real testing against Google OAuth, Supabase, FullCalendar, and the service worker before shipping, and guessing at one blind risked breaking the app in a way that wouldn't surface until deployed.
- **Medium**: the `task-attachments` Storage bucket had no `file_size_limit` or `allowed_mime_types` (unbounded upload size/type) — added a 25MB cap and a document/image allowlist in migration `0020`.
- **Clean**: no hardcoded secrets, no XSS vectors (React escapes all interpolated text by default; the one `dangerouslySetInnerHTML` is a static string), `npm audit` reports zero vulnerabilities, `friendlyError()` already prevents raw DB errors from reaching the browser, and the `next` param in the OAuth callback isn't an exploitable open redirect (string-concatenated onto `origin`, never URL-parsed).

**Migrations `0019` and `0020` need to be run in Supabase** like every other migration (see the gotcha below) — as of this writing, not yet confirmed run.

## Application-level encryption (this session)

At the client's request, sensitive content is now encrypted **in application code** before it ever reaches Supabase — not via Postgres/`pgcrypto`. This distinction matters: the explicit threat model is "defense in depth if RLS fails again, or if the service role key leaks" — encrypting inside Postgres would mean the key has to be reachable from SQL, which defeats protecting against exactly that second scenario. The key lives only in `ENCRYPTION_KEY` (`.env.local`, 32-byte AES-256 key, base64), server-side only, never sent to the client.

**What's covered**: task titles, task descriptions, task comments, announcement title/body, and attachment file bytes. **What's explicitly not covered** (out of the scope the client chose): calendar event/meeting titles, profile names/emails, task metadata (status, dates, file names/sizes/MIME types — these stay plain since the app needs to query/sort/filter/display them without decrypting, and the client didn't flag them as sensitive).

**Core utility**: `src/lib/encryption.ts` — AES-256-GCM (authenticated; tampering is detected, not silently accepted).
- `encrypt`/`decrypt` (+ `Nullable` variants) for text columns: stored as `"<iv>.<authTag>.<ciphertext>"`, all base64, in the existing `text` column — no schema change needed.
- `encryptBuffer`/`decryptBuffer` for attachment file bytes: raw `[iv][authTag][ciphertext]` layout, since these go straight into Storage rather than a Postgres column.

**Backward compatibility with existing (pre-encryption) data**: `decrypt()` for text fields gracefully falls back to returning the raw value when it isn't in the expected 3-part shape or fails to decrypt — so task/comment/announcement text written before this change keeps displaying correctly, while everything written from now on is encrypted. **Attachments have no such fallback** — there's no reliable way to distinguish "old plaintext file bytes" from "not our format" the way there is for text, so files uploaded before this change will 410 on download with an explanatory message. Given this project currently only has test data, that tradeoff was accepted rather than building a migration path.

**Where encrypt/decrypt happens**: encryption happens once, right before each RPC call in the Server Action (`createTask`, `postAnnouncement`/`updateAnnouncement`, `addComment`, the attachment upload loop in `tasks/actions.ts`). Decryption happens at every read site — `src/lib/queries.ts` (`getTaskList`, `getTaskDetail`), `src/lib/dashboard-queries.ts` (`getRecentlyCompleted`, `getOverdueAndBlockedTasks`, `getUpcomingDeadlines`, `getAnnouncements`), `calendar/page.tsx`, `history/page.tsx`. **If you add a new query that selects `title`/`description`/`body` from `tasks`, `task_comments`, or `announcements`, you must decrypt it there** — nothing enforces this at the type level, it's a manual convention. Everything downstream of these functions (components, other pages) receives already-decrypted plaintext and needs no changes.

**Attachments — architecture change**: file bytes are encrypted before upload (still via the service-role client, same as before). This broke the previous download flow (`createSignedUrl` + direct `<a href>` to Supabase Storage), since that would just hand the browser ciphertext. Replaced with a Route Handler, `src/app/(app)/tasks/[id]/attachments/[attachmentId]/route.ts`, which checks authorization against the `task_attachments` table (respects RLS via `can_view_task`), fetches the encrypted bytes with the service-role client, decrypts, and streams the real file back with the original filename/MIME type (kept as plaintext metadata). The old `getAttachmentUrl` action and signed-URL logic were removed as dead code.

**Gotcha that already bit once — Storage MIME allowlist vs. encryption**: migration `0020` (security review) added a document/image MIME allowlist to the bucket; encryption (added later the same session) made every upload go through as `application/octet-stream` regardless of the real file type, since Storage only ever sees ciphertext now. Every upload failed (`mime type application/octet-stream is not supported`) until `0021` relaxed the bucket to accept `octet-stream`, with the *real* type/size validation moved into application code (`ALLOWED_ATTACHMENT_MIME_TYPES`/`MAX_ATTACHMENT_BYTES` in `tasks/actions.ts`, checked before encryption while the real `file.type` is still visible). **Lesson for future work**: any bucket-level Storage restriction (MIME type, etc.) is checked against what's actually uploaded — once content is encrypted before upload, that's always ciphertext, so this class of restriction has to live in application code instead, checked before encrypting.

## Email allowlist replaces the Workspace domain check (this session)

The client clarified this company has no Google Workspace — employees sign in with personal Gmail-type addresses. The domain check (`auth/callback/route.ts`) is a no-op when `NEXT_PUBLIC_GOOGLE_WORKSPACE_DOMAIN` is blank, which meant **anyone with any Google account could sign in**, and since `profiles_select` RLS is `to authenticated using (true)`, they'd immediately be able to read the full staff directory (every name/email/role/department) — a real PII exposure, not just an unassigned-employee annoyance, discovered while scoping out what "get everyone into the system" actually required.

Fix: `allowed_emails` table (`0022_email_allowlist.sql`), gated entirely behind `SECURITY DEFINER` RPCs (no plain RLS policy on the table at all — matches the established convention). `is_email_allowed(email)` is checked in the OAuth callback alongside the (now effectively unused, but harmless to leave) domain check; anyone not on the list gets signed back out immediately, same as a wrong-domain account. The migration grandfathers in every email that already has a `profiles` row, so existing test accounts (including the President's) aren't locked out. Managed from a new "Allowed emails" card at the top of the Accounts page (`AllowedEmails` component) — boss_boss/supervisor only, add/remove backed by `add_allowed_email_rpc`/`remove_allowed_email_rpc`.

**Operational implication**: before pointing a new person at the sign-in page, their email needs to be added here first — otherwise they'll hit a "not approved yet" error on their first sign-in attempt. Worth being explicit about this in the SOP, since it changes the onboarding order from "let them sign in, then set them up" to "add their email first, then let them sign in."

## Mobile responsiveness + dashboard UI balance (this session)

- **Sidebar**: was a permanently-docked 240px column with no responsive handling — unusable on a phone. Restructured into `src/components/nav/app-shell.tsx` (client component holding open/close state) rendered from the layout; below the `lg` breakpoint the sidebar becomes an off-canvas drawer opened via a hamburger button in `TopBar`, with a backdrop and auto-close on navigation.
- **Calendar**: a full 7-column month grid has no usable width on a phone (event titles truncated to 1-2 characters). `calendar-view.tsx` now imperatively switches to `listWeek` on mount if the viewport is under 640px (FullCalendar's `initialView` prop only applies once, so this can't be done declaratively) — deliberately only on mount, not on every resize, so it doesn't fight a view someone picks manually mid-session. The header toolbar also gets `flex-wrap` under 640px in `globals.css` since three button groups don't fit one row at phone width.
- **Accounts table**: 4 columns don't fit mobile width without truncating role/department to a couple of characters. Now renders two parallel layouts — the table (`hidden md:block`) and a stacked card per person (`md:hidden`) sharing the same state/handlers.
- **History filter form**: fields now take full width on mobile (`w-full sm:w-auto`) instead of an unpredictable content-based width. Note: the native `<select>` dropdown list itself (what you see when tapping "Department") is OS/browser-rendered, not something this app's CSS can restyle — flagged to the client as expected behavior, not a bug, with the option to build a custom dropdown component if it's worth the effort later.
- **Dashboard card balance**: cards in the same grid row weren't equal height (`Card` component didn't stretch to fill its grid cell), which read as inconsistent. First fix (`h-full` on every `Card`) was reverted after the client flagged it looking worse for badly-mismatched pairs (a 2-stat completion-rate card force-stretched to match an 11-department list left huge dead space). The actual fix: don't force mismatched content to match heights at all — `department_health` and `completion_rate` were made full-width rows (`lg:col-span-2` in `dashboard/page.tsx`) instead of being paired 50/50 with something a different natural size. `department-health-grid.tsx` also had truncated department names (`truncate` class cutting off names like "Mine Environmental Protection and Enhancement Department") — switched to a wrapping list instead of a pill grid.

## Commit/deploy status

Everything through this session (delete-task-button, announcement edit/delete, calendar fixes, emoji removal, font-size bump, mobile responsiveness, dashboard balance fixes, the security review fixes, the encryption work, the History deadline filter, the chain-step validation fix, the email allowlist, this doc) is committed as a single commit on `main` and pushed to `github.com/Joooban/anri_tasks` — see the Deployment section above. `.env.local` was confirmed never tracked/staged (gitignored) before committing. Working tree was clean as of the last push.

## Known gaps / not yet done

- Both `SUPABASE_SERVICE_ROLE_KEY` and `ENCRYPTION_KEY` are now set in Vercel (server-only, not `NEXT_PUBLIC_`) as well as local `.env.local`. **`ENCRYPTION_KEY` still needs a durable backup outside both of those** (a password manager) — if it's lost, every encrypted row becomes permanently unreadable with no recovery path. Not yet confirmed done.
- All migrations `0001`–`0022` confirmed applied to the Supabase project this deployment uses, including `0022` (email allowlist) — initially missed when deployment happened (built but never handed to the client to run), caught when production sign-in failed closed with "not approved yet" for every account. Lesson: a migration existing in the repo isn't the same as it being run — this class of bug has now happened three times this project (see the "migrations are not auto-applied" gotcha above), reinforcing that this is the single highest-value thing for the SOP to formalize.
- Onboarding still needs each new person's email added to the allowlist before their first sign-in attempt (see Deployment section) — not yet done for the actual team, deferred by the client for now.
- A Content-Security-Policy header was deliberately not added — needs real testing against Google OAuth/Supabase/FullCalendar/the service worker first.
- Org chart sub-units unseeded (blocked on client input, not a code issue).
- Task type management is still SQL-editable only, no admin UI.
- No generated TypeScript types from the database (`supabase gen types typescript` never run) — types are hand-maintained in `src/lib/types.ts`.
- Migrations `0023`–`0026` (task-status fix, dashboard widget cleanup, profile birthday columns, document templates) confirmed applied to production — see the client-feedback-batch section above.
- Custom/self-defined admin roles & permissions: deliberately not started, no scope decision made yet. (Task-sharing via link was resolved this session — see its own section above; scoped internal-only, no allowlist changes.)

## For the SOP itself

Given the above, likely candidates for what the SOP should cover:
- The migration-must-be-manually-run gotcha (highest-value one to formalize — has caused real outages more than once).
- The RPC-over-RLS convention for any new write path, and why — including the Storage exception (service-role client) for the one write path that can't go through a Postgres RPC, and the lesson from this session's security review that the *original* policy behind an RPC must be dropped, not just left inert.
- Handling of secrets specifically (`SUPABASE_SERVICE_ROLE_KEY`, `ENCRYPTION_KEY`): never client-side, never `NEXT_PUBLIC_`, both need a real production home and `ENCRYPTION_KEY` needs a backup plan before ship.
- The Boss/Supervisor visibility-only principle, as a design rule for any future feature review.
- The encryption convention: any new query touching `title`/`description`/`body` on `tasks`/`task_comments`/`announcements` must decrypt at the read site — not enforced by the type system, easy to miss.
